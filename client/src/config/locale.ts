/**
 * The one place that decides how money and dates look. The data carries no currency, so the app
 * assumes US dollars; changing these two values re-formats every amount and date in the UI.
 */
export const LOCALE = 'en-US';
export const CURRENCY = 'USD';
