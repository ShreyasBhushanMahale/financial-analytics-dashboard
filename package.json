{
  "name": "ledgerline",
  "version": "0.1.0",
  "private": true,
  "description": "Financial analytics dashboard: JWT auth, filterable transactions, charts and CSV export.",
  "workspaces": [
    "server",
    "client"
  ],
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "concurrently --names server,client --prefix-colors green,yellow \"npm run dev -w server\" \"npm run dev -w client\"",
    "seed": "npm run seed -w server --",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "test": "npm run test --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "concurrently": "^9.2.4",
    "prettier": "^3.9.8"
  }
}
